- if organization_owner?
  %div Invite a user to this organization:

  = form_tag invite_members_path do
    = text_field_tag 'email', '', class: 'w30', placeholder: 'i.e. john@doe.com'
    = submit_tag 'Invite'

  %br
  %br

- if members.empty?
  - if current_organization
    %div.info There are no members in this organization
  - else
    .missions_empty
      %div.info To start using Watchfire you must create an Organization
      %a.add{:href => new_organization_path}
        Create Organization
        %button.cadd
- else
  %div Current members:

  %ul
    - members.each do |member|
      %li= member.user.email
